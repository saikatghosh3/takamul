# T2Hub & SVP Scraper API — কার্যকর গাইড (বাংলা)

---

## ১. সিস্টেম ওভারভিউ ও আর্কিটেকচার

### মূল উদ্দেশ্য

T2Hub (t2hub.app) হলো একটি থার্ড-পার্টি এজেন্সি ম্যানেজমেন্ট মিডলওয়্যার যা Saudi SVPI / Takamol (svp-international.pacc.sa) পোর্টালের উপর নির্মিত। এটি রিক্রুটমেন্ট এজেন্সিগুলোকে উচ্চ-পরিমাণ ক্যান্ডিডেট বুকিং প্রক্রিয়াকরণে সাহায্য করে।

**মূল সমস্যা:** সরাসরি svp-international.pacc.sa ওয়েবসাইটে হাতে কলমে প্রতিটি ক্যান্ডিডেটের তথ্য প্রবেশ করা, OTP যাচাই করা, এবং পরীক্ষার কেন্দ্র খোঁজা অত্যন্ত সময়সাপেক্ষ। T2Hub এই প্রক্রিয়াগুলো স্বয়ংক্রিয় করে।

### টেক স্ট্যাক

| স্তর | প্রযুক্তি |
|------|-----------|
| **ফ্রন্টএন্ড** | Next.js 16 + React 19 + Tailwind CSS 4 |
| **ব্রাউজার অটোমেশন** | Puppeteer (headless Chrome) |
| **ব্যাকএন্ড API** | Next.js Route Handlers (App Router) |
| **ক্যাশিং** | In-memory Map cache (TTL-based) |
| **টার্গেট পোর্টাল** | svp-international.pacc.sa (Vue.js SPA) |
| **টার্গেট API** | svp-international-api.pacc.sa (Laravel REST API) |

### ইন্টিগ্রেশন পয়েন্ট

```
┌─────────────────┐     ┌──────────────────────┐     ┌─────────────────────┐
│   T2Hub Agent   │────▶│  pacc-scraper-api    │────▶│  SVP International  │
│   Dashboard     │     │  (Next.js + Puppeteer)│     │  (svp-international │
│  (t2hub.app)    │     │                      │     │   .pacc.sa)         │
└─────────────────┘     └──────────────────────┘     └─────────────────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │  svp-international-  │
                    │  api.pacc.sa         │
                    │  (Backend REST API)  │
                    └──────────────────────┘
```

**প্রধান API এন্ডপয়েন্ট (svp-international-api.pacc.sa):**

- `POST /api/v1/sessions/otp` — OTP পাঠানোর জন্য (phone_number + recaptcha প্রয়োজন)
- `GET /api/v1/visitor_space/categories` — পরীক্ষার ক্যাটাগরি তালিকা
- `GET /api/v1/visitor_space/test_centers` — পরীক্ষার কেন্দ্র তালিকা
- `GET /api/v1/individual_labor_space/exam_sessions/available_dates` — উপলব্ধ তারিখ
- `GET /api/v1/individual_labor_space/test_centers/cities` — শহরের তালিকা

---

## ২. ওয়ার্কফ্লো ও অটোমেশন ইঞ্জিন

### ক্যান্ডিডেট প্রসেসিং (Takamol Entry)

```
ইউজার ইনপুট (Category + City + Date)
        │
        ▼
┌─────────────────────────┐
│  ১. Category Select     │  → /api/takamol/categories (Puppeteer দিয়ে encryptedFetch)
│  বা /api/search (GET)   │     visitor_space/categories থেকে ক্যাটাগরি আনা হয়
└─────────────────────────┘
        │
        ▼
┌─────────────────────────┐
│  ২. City & Date Load    │  → /api/takamol/dates (Puppeteer দিয়ে)
│                         │     individual_labor_space/exam_sessions/available_dates
│                         │     থেকে উপলব্ধ তারিখ ও শহর আনা হয়
└─────────────────────────┘
        │
        ▼
┌─────────────────────────┐
│  ৩. Center Search       │  → /api/search (POST)
│                         │     visitor_space/test_centers থেকে সব কেন্দ্র পেজিনেশন সহ আনা হয়
│                         │     ফলাফল: center name, address, city
└─────────────────────────┘
        │
        ▼
┌─────────────────────────┐
│  ৪. Calendar UI         │  → React Calendar Component
│                         │     উপলব্ধ তারিখগুলো সবুজ দিয়ে দেখানো হয়
│                         │     অনুপলব্ধ তারিখ স্লেট-৬০০ রঙে দেখানো হয়
└─────────────────────────┘
```

### OTP ইন্টারসেপশন লুপ

T2Hub প্ল্যাটফর্ম **DakBox** (dakbox.net) ব্যবহার করে OTP অটো-ফেচিং করে:

```
┌──────────────────────────────────────────────────────────┐
│  ১. ক্যান্ডিডেটের ফোন নম্বর দিয়ে SVP ওয়েবসাইটে       │
│     OTP পাঠানো হয় → POST /api/v1/sessions/otp          │
│     বডি: {phone_number, recaptcha}                      │
│                                                          │
│  ২. DakBox Chrome Extension বা API দিয়ে                │
│     ইমেইল ইনবক্স অটো-চেক করা হয়                        │
│                                                          │
│  ৩. OTP কোড পাওয়া গেলে সেটি অটোমেটিকভাবে               │
│     SVP পোর্টালে পপুলেট করা হয়                          │
│                                                          │
│  ৪. মানুষের হস্তক্ষেপ ছাড়াই লগইন সম্পন্ন হয়             │
└──────────────────────────────────────────────────────────┘
```

**DakBox এর সুবিধা:**
- Yopmail এর মতো প্রতিবার captcha পূরণ করার ঝামেলা নেই
- OTP ছাড়াই svp-international.pacc.sa-তে লগইন ও রেজিস্ট্রেশন সম্ভব
- Chrome Extension ব্যবহার করে সরাসরি ব্রাউজারে OTP অটো-ফিল হয়

### পোর্টাল সাবমিশন

```
┌──────────────────────────────────────────────────────────┐
│  Puppeteer ব্রাউজার অটোমেশন:                            │
│                                                          │
│  ১. নতুন ব্রাউজার সেশন খোলা হয় (headless Chrome)       │
│  ২. svp-international.pacc.sa/ এ নেভিগেট করা হয়       │
│  ৩. page.evaluate() দিয়ে পেজ কনটেক্সটে                │
│     encryptedFetch() কল করা হয় (CORS বাইপাস)          │
│  ৪. API রেসপন্স পাওয়া গেলে JSON parse করা হয়           │
│  ৫. ক্যাশে সংরক্ষণ করা হয় (5-30 মিনিট TTL)             │
└──────────────────────────────────────────────────────────┘
```

**মূল কৌশল:** `takamol.js` ফাইলে `ensurePage()` ফাংশনটি Puppeteer দিয়ে একটি headless Chrome ব্রাউজার চালু করে, SVP ওয়েবসাইটে নেভিগেট করে, এবং সেই পেজের JavaScript কনটেক্সট থেকে `encryptedFetch()` ফাংশন কল করে। এভাবে CORS ও WAF সীমাবদ্ধতা বাইপাস হয়।

---

## ৩. কোটা ও আর্থিক সাবসিস্টেম

### কোটা লাইফসাইকেল

```
┌──────────────────────────────────────────────────────────┐
│  ১. এজেন্ট রেজিস্ট্রেশন → প্রাথমিক কোটা বরাদ্দ          │
│     (1 Unit = 1 Processed Entry)                         │
│                                                          │
│  ২. ক্যান্ডিডেট এন্ট্রি সাবমিট হলে → 1 Unit কাটা হয়    │
│                                                          │
│  ৩. কোটা শেষ → নতুন এন্ট্রি সাবমিট অক্ষম               │
│                                                          │
│  ৪. পেমেন্ট করলে → কোটা রিচার্জ হয়                     │
└──────────────────────────────────────────────────────────┘
```

### কনকারেন্সি ও রেস কন্ডিশন

- **সীট হোল্ড টাইমআউট:** পোর্টালে সীট বুক করলে একটি নির্দিষ্ট সময়ের জন্য সীট হোল্ড থাকে
- **বুকিং এক্সপায়ারি:** পোর্টাল সাবমিশনের সময় যদি সেশন মেয়াদোত্তীর্ণ হয়, তাহলে অটোমেটিকভাবে রিট্রাই বা এরর হ্যান্ডলিং হয়
- **কোটা ব্যালেন্স প্রোটেকশন:** একই সাথে একাধিক স্টাফ ইউজার এন্ট্রি করলে নেগেটিভ কোটা ব্যালেন্স প্রতিরোধ করতে optimistic locking বা atomic decrement ব্যবহার করা হয়

### পেমেন্ট গেটওয়ে

```
┌──────────────────────────────────────────────────────────┐
│  অনলাইন পেমেন্ট (Instant):                              │
│  ──────────────────────────                              │
│  ১. এজেন্ট ড্যাশবোর্ড থেকে "রিচার্জ" বাটনে ক্লিক       │
│  ২. পেমেন্ট গেটওয়ে (যেমন PayTabs/Moyasar) এ রিডাইরেক্ট│
│  ৩. পেমেন্ট সফল হলে গেটওয়ে থেকে callback/webhook আসে   │
│  ৪. T2Hub সার্ভার callback ভেরিফাই করে                  │
│  ৫. কোটা অটোমেটিকভাবে রিচার্জ হয়                       │
│                                                          │
│  ব্যাংক ট্রান্সফার (Manual Verification):               │
│  ────────────────────────────────────────                │
│  ১. এজেন্ট ব্যাংক ট্রান্সফার করে                         │
│  ২. ট্রান্সফার স্লিপ/রিসিট T2Hub এ আপলোড করে            │
│  ৩. অ্যাডমিন ম্যানুয়ালি ভেরিফাই করে                     │
│  ৪. ভেরিফাই হলে ম্যানুয়ালি কোটা যোগ করা হয়             │
└──────────────────────────────────────────────────────────┘
```

---

## ৪. রোল-বেজড অ্যাক্সেস কন্ট্রোল (RBAC) ও অডিট লগিং

### পারমিশনস ইঞ্জিন

```
┌──────────────────────────────────────────────────────────┐
│  Master Agent (মাস্টার এজেন্ট):                          │
│  ────────────────────────────                            │
│  • সব ধরনের পারমিশন আছে                                 │
│  • সাব-এজেন্ট তৈরি ও ম্যানেজ করতে পারে                   │
│  • কোটা বিতরণ করতে পারে                                 │
│  • পেমেন্ট ম্যানেজ করতে পারে                              │
│                                                          │
│  Admin (অ্যাডমিন):                                       │
│  ─────────────────                                       │
│  • ক্যান্ডিডেট এন্ট্রি তৈরি/এডিট/ডিলিট করতে পারে       │
│  • মেনু ম্যানেজার অ্যাক্সেস থাকতে পা�রে                    │
│  • রিপোর্ট দেখতে পারে                                   │
│                                                          │
│  Staff (স্টাফ):                                          │
│  ──────────                                              │
│  • শুধুমাত্র ক্যান্ডিডেট এন্ট্রি তৈরি করতে পারে          │
│  • নিজের এন্ট্রি দেখতে পারে                              │
│  • পেমেন্ট বা সেটিংস অ্যাক্সেস নেই                        │
└──────────────────────────────────────────────────────────┘
```

**এনফোর্সমেন্ট:** Middleware/API রুটে JWT token থেকে role বের করে, তারপর প্রতিটি API কলের আগে permission check করা হয়।

### অডিট ট্রেইল (My Logs)

প্রতিটি অটোমেশন অ্যাক্সেনের নিচের তথ্যগুলো লগ করা হয়:

| ফিল্ড | বিবরণ |
|-------|-------|
| **Candidate Passport** | পাসপোর্ট নম্বর |
| **Booking ID** | বুকিং আইডি (যদি থাকে) |
| **Timestamp** | কাজ করার সময় |
| **Operator ID / Done By** | কাজ করেছেন কে (ইউজার আইডি) |
| **Action Type** | Create / Edit / Delete |
| **Status** | Success / Failed / Pending |
| **API Response** | SVP পোর্টাল থেকে প্রাপ্ত রেসপন্স |

---

## ৫. সিকিউরিটি, এজ কেস ও এরর হ্যান্ডলিং

### সেশন এক্সপায়ারি ও সীট লক

```
┌──────────────────────────────────────────────────────────┐
│  সমস্যা: পোর্টালে সীট হোল্ড করার পর বুকিং এক্সপায়ারি   │
│                                                          │
│  সমাধান:                                                │
│  ১. Puppeteer সেশন মনিটরিং করে                          │
│  ২. সেশন এক্সপায়ারি হলে অটোমেটিক রিট্রাই লজিক          │
│  ৩. নতুন সেশন তৈরি করে আবার চেষ্টা করে                  │
│  ৪. সর্বোচ্চ রিট্রাই সীমা পাস হলে এরর লগ করে থামায়      │
└──────────────────────────────────────────────────────────┘
```

### ফেইলিউর রিকভারি

```
┌──────────────────────────────────────────────────────────┐
│  OTP API ব্যর্থ হলে:                                     │
│  ──────────────────                                      │
│  • 3-5 বার রিট্রাই করে (exponential backoff)             │
│  • রিট্রাই ব্যর্থ হলে ইউজারকে "OTP পাঠানো যায়নি" মেসেজ  │
│  • DakBox ব্যবহার করলে alternative OTP পাঠানোর চেষ্টা     │
│                                                          │
│  পোর্টাল এরর দিলে:                                      │
│  ─────────────────                                       │
│  • HTTP স্ট্যাটাস কোড চেক (429 = Rate Limit)             │
│  • 429 হলে কিছু সময় অপেক্ষা করে আবার চেষ্টা             │
│  • 500 হলে সার্ভার সমস্যা, লগ করে ইউজারকে জানায়         │
│                                                          │
│  পেমেন্ট ব্যর্থ হলে:                                     │
│  ──────────────────                                      │
│  • গেটওয়ে callback আসেনি → পেন্ডিং স্ট্যাটাস             │
│  • ইউজারকে "পেমেন্ট যাচাই করা হচ্ছে" মেসেজ দেখায়        │
│  • অ্যাডমিন প্যানেলে pending payment তালিকায় যোগ হয়     │
│  • ম্যানুয়াল ভেরিফিকেশনের অপশন থাকে                     │
└──────────────────────────────────────────────────────────┘
```

### কোডে ব্যবহৃত কৌশলসমূহ

```javascript
// takamol.js — মূল Puppeteer অটোমেশন লজিক
// ১. হেডলেস Chrome চালু করে
browser = await pup.default.launch({
  headless: 'new',
  args: ['--no-sandbox', '--disable-setuid-sandbox']
});

// ২. User-Agent সেট করে (ব্লক এড়াতে)
await page.setUserAgent('Mozilla/5.0 (Windows NT 10.0; Win64; x64)...');

// ৩. SVP ওয়েবসাইটে নেভিগেট করে
await page.goto('https://t2hub.app/takamol', { waitUntil: 'networkidle2' });

// ৪. পেজ কনটেক্সটে API কল (CORS বাইপাস)
const result = await p.evaluate(async (apiPath) => {
  const res = await encryptedFetch(apiPath);  // SVP-এর নিজের encryptedFetch
  return await res.json();
}, path);

// ৫. ক্যাশিং (5-30 মিনিট TTL)
cache.set(path, { data: result, time: Date.now() });
```

```javascript
// search/route.js — টেস্ট সেন্টার সার্চ
// পেজিনেশন সহ সব কেন্দ্র আনা হয়
while (page <= totalPages && page <= 30) {
  const res = await fetch(`${API_BASE}/visitor_space/test_centers?...`);
  all.push(...(data.test_centers || []));
  totalPages = data.pagination?.pages || 1;
  page++;
}
```

---

## গুরুত্বপূর্ণ নোট

1. **এই কোডবেস শুধুমাত্র SVP পরীক্ষার কেন্দ্র খোঁজার জন্য ব্যবহৃত হয়** — এটি ক্যান্ডিডেট এন্ট্রি সাবমিশন বা OTP অটো-ফেচিং করে না।
2. **T2Hub প্ল্যাটফর্ম (t2hub.app) আলাদা একটি সম্পূর্ণ সিস্টেম** যার সোর্স কোড এখানে নেই — শুধু তাদের পাবলিক পেজ থেকে তথ্য সংগ্রহ করা হয়েছে।
3. **svp-international.pacc.sa** হলো মূল Takamol/SVP পোর্টাল যেটি একটি Vue.js SPA এবং এর ব্যাকএন্ড API `svp-international-api.pacc.sa`।
4. **DakBox** একটি তৃতীয় পক্ষের ইমেইল সার্ভিস যা OTP অটো-ফেচিং সক্ষম করে।
